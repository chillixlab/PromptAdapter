[env]
_.python.venv = { path = ".venv", create = true }
_.file = ".env"

[tools]
python = "3.12"
uv = "latest"

[tasks.init]
description = "Install dependencies"
run = "uv sync --all-extras"

[tasks.test]
description = "Run tests"
run = "uv run pytest tests"

[tasks.lint]
description = "Run linters"
run = "uv run ruff check . --exclude experiments && uv run ty check prompt_adapter tests scripts"

[tasks.format]
description = "Format code"
run = "uv run ruff check --fix . --exclude experiments && uv run ruff format . --exclude experiments"
